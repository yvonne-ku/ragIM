yep, you've got it. where's the PYTHONPATH stored? actually, just PATH i wonder if sys.path concatenates both yeah, sys.path is pythonpath right, and it is set at every interpreter initialisation. have you specified a startup file for your interpreter? i use fish, i have a custom fishrc but i didn't set the pythonpath, so it has to be somewhere else do you have an PYTHONPATH environmental variable set? possibly in .login or .bashrc? i grepped my entire home directory, nothing's set