state your problem. Well answer if we can drivers for what? what's your video card? Doesn't work well by default? not necessarily. some commands aren't verbose by default so is there a way to figure out if I've executed to commands correctly? if another lines comes awating orders (the same that appears when you open a terminal) it means it was executed. However you won't know how it exited. Better would be too look for the verbose option in the man pages of the command (but usually is -v) I'm thinking that I probably need to get a little more familiar with the commands for terminal as most of what you just told me went over my head man pages are something really uselful. Just enter man name_of_command in the terminal and it will explain you how to use that command ahh usually a command syntax is something like 'command -options -something_else' By adding -v in the options you sometimes activate verbose mode of the command but always check the man pages of that command to check so I wouldn't necessarily use '-v' without checking if it is supported, correct? would there be any danger in re-doing my driver installs as I'm not sure if they took? yes. Search the man page of the command to see if -v activates verbose mode. In can do something else, you never know. ALso remember that it's case sensitive so -v is different that -V i have noticed that stuff is case sensitive