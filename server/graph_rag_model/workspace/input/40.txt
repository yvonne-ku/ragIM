no error in the file.. :( were you able to run aptitude? same issue (aptit. runs, but has same error) you can't deselect (':') the upgrade? no, ive run upgrades many many times. AFAIK the system thinks the upgrades have taken hold and it does look like a smiley--small moustache just the kernel-upgrade, not the rest im not sure how to do that? I run sudo aptitude, then shift+;? press g once, that should take you to the list of packages changes. Then select each line and press shift+; when you are done, press g, again. If nothing happens quit. then try aptitude -f install it does not let me deselect, since they are partially installed the only packages that need installing, it says, are the kernel updates and nspluginwrapper (which I haven't used since I use 64-bit flash) do you have multiple kernels installed?