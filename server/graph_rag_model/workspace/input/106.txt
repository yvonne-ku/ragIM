k iptables has no policy defined by default default policy on my install is none there's no such thing as that check the output of iptables -L I just did so you have a line like Chain INPUT (policy DROP) no then the iptables kernel modules aren't loaded you're playing with fire ;) what ? it's bad not to have any firewall whatsoever I have network related firewalls, not host based and thats the default ubuntu install