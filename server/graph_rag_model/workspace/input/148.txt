yes on the laptop. It didn't helt to set a mrk in front of the use-ssh-agent using: sudo nano /etc/X11/Xsessions.options. I still could log in without being asked to enter the password the second time I tried. And I have restarted X at my laptop since I did the change. hey, remember the installing of libsdl1.2-dev? you don't have the dev package for sdl installed according to ubuntu it is try reinstalling then ok actually, it's because it's looking in /usr/local instead of /usr, probably run ./configure --help and set the prefix for SDL to be /usr instead of /usr/local got the same error is it still looking in /usr/local? it's looking in /usr try 'which sdl-config' if it doesn't find it, then you don't have the -dev installed /usr/local/bin/sdl-config oh, then it is in local...did you compile that by source, too? I believe I got it from the repos then it would be in /usr/bin instead of /usr/local/bin then I got them from sorce source and packages don't work well together run sdl-config --version but if I yank the libsdl1.2-dev out alot of my packages well stop working 1.2.13 Couldn't stat source package list http://security.ubuntu.com breezy-security/universe Packages <--- what could be wrong ?! sudo apt-get update