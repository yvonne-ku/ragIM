did you solve it? let it to use ALSA, just make sure it doest use /dev/dsp. check out my ubuntu chronicles page at http://pykeylogger.sourceforge.net/wiki/index.php/Ubuntu:Chronicles (the sound problems section) yea, dont use the sound device directly. that's what alsa and esd sound daemons are for. cool :) I downloaded cd-track-reader and ran ./configure, but it says libglade-2.0 is missing. I have libglade2-0 installed though. it didn't see libglade-2.0, only libglade2-0? Looks like a typo in the configure script (unless *you* mistyped the dashes when you copied it here) Nope, I copy-pasted then watch out for a typo in the configure script (a misplaced dash), ok? Ok, I figured it was a slightly different variant from what I already had installed. that's possible I suppose, but I would shoot the developer who decided to release two variants of something, differing only in the location of the dash.... I think you should shoot them then :) I installed libglade2-0 and libglade2-dev and it configured. sigh.... Care to work through the make errors with me? http://pastebin.com/d5c9026c0 libisofs/libisofs.h: you're missing a -dev package. libisofs4-dev on my hardy system. Fix this first then try again.... Thanks, that did it.