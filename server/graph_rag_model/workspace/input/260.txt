So it works? Any time. are you still having trouble - I found info on configuring /etc/fetchmailrc yeah, i am, actually - apparently, ubuntu uses a completely different way of running fetchmail than normal there's a sample config file on this page: https://help.ubuntu.com/community/GmailPostfixFetchmail?highlight=%28%2Fetc%2Ffetchmailrc%29 i want to run it as a daemon at startup and poll different servers.. i'm thinking the solution might be to run it as a user in cron at startup, since it needs to use the user level .fetchmailrc, but it feels like a hack this page looks exactly like what i wa slooking for - what did you google for? I didn't google - I used the ubuntu wiki like I would google and did a text search for /etc/fetchmailrc well, it's sorta similar...i'm going to use procmail instead of postfix ok...yeah, /etc/fetchmailrc is an Ubuntu thing, so the man pages are worthless ...yeah, this page does the same thing - it uses the /etc/fetchmailrc on a user basis, so it only works for a single user - you have to put your username/password in that file....dumb hey i have a script i want to run it when the system starts up and then roughly every 6 hours