yeah have you tried adding 'ide_disk' and 'ide_cd' to /etc/modules? haven't tried that once I add ide_cd and ide_disk, do I need to reboot ubuntu? once I had to do something like that but through a BusyBox prompt at boot time to load the modules so I need to restart to make the changes? or can I run a command to reload the modules? try rebooting ok, back in a sec sudo modprobe ide_disk sudo modprobe ide_cd the thing is that if the modules are not loaded at boot time what do you think of the error message? you have nvidia binaries installed beyond what ubuntu offers - rectify that and you should be fine you mean using default repos for them? you mean to return to default repos (before)? I mean not installing nvidia binaries that will break on your kernel version :) that 99.xxmilliongazillions you';re using is even friggin beta on Windoze i feat that if i remove them, my aiglx/beryl might not work anymore :-( I will guarantee you that they won't work, until you re-install the ubuntu drivers - why did you change them ? because it was written in a howto and i tought they were the newest. so what can i do to KEEP beryl? you don't need to do anything - just uninstall the alien nvidia binary and install the normal ubuntu one that's what i'll do; but do you think beryl won't work then anymore? I am telling you that it works for me; it *might* not work if you have a $500 nvidia monster that's say less than 6 months old well i havent (6600 :-))