Glad I helped you! :) You can do hostname.local You could optionally set up a DNS server (perhaps not worth the effort) as in, ssh username@hostname.local? Yes.  That should work on base installs Nope, not working Sure, you can edit the hostname (not a really good solution) but many people just use IP address on little local networks Nono; I mean, ssh username@hostname.local gets 'Name or service not known', but ssh username@192.... works just fine I know that isn't working (I thought it might). I'm saying that you can edit the /etc/hosts file as well. But many people just use IP addresses.  That's totally normal on a small network. sure