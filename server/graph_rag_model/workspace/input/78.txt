oh sudo killall synaptic should kill it but leave the lock file. you can remove that one afterwards. then you should be able to use apt run the sudo aptitude install apache2, and its running it start in installing it's sudo aptitude install apache2 though how ca I test it /etc/init.d/apache2 start the command I just suggested should start apache. provided it succeeds and you don't have a firewall blocking connections you should then be able to see the default apache page on the server This is the message, Unable to open logs from what? upon executing the /etc/init.d/apache2 statr sudo /etc/init.d/apache2 start. note the sudo httpd already running good. that means it's already started. I think try sudo /etc/init.d/apache2 restart to make sure Forcing reload of web server ok. good. now lynx locahost sudo aptitude install lynx if you don't have it or try w3 instead w3m* would I use a browser? yeah ;) w3m and lunx are terminal browsers IRCMonkey.executing sudo aptitude lynx sudo aptitude install lynx IRCMonkey, yes anf its installing yes anf its installing good hey its done lynx localhost then it should show the default apache page